wb = xlsx_package.workbook
wb.add_worksheet(name: truncate(@table.name, lenght: Constants::NAME_TRUNCATION_LENGTH).strip) do |sheet|
  table_data = JSON.parse(@table.contents)["data"]
  table_data.each do |row|
    sheet.add_row row
  end
end